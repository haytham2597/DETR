#pragma once

#ifndef LIBS_INTERMEDIATE_LAYER_GETTER_H
#define LIBS_INTERMEDIATE_LAYER_GETTER_H

#include <torch/torch.h>

/*class IntermediateLayerGetter : public torch::nn::ModuleList
{
public:
	IntermediateLayerGetter(torch::nn::Module model, torch::Dict<std::string, std::string> return_layers)
	{
		torch::OrderedDict<std::string, torch::nn::Module> layers;
		for(auto v : model.named_children())
		{
			
		}
	}
};*/

#endif